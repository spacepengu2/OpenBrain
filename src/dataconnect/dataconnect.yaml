specVersion: "v1beta"
serviceId: "openbrain"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "openbrain-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
